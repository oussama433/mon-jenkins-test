pipeline {
    agent any
    
    environment {
        DOCKER_IMAGE = 'oussama433/student-project'
        SCANNER_HOME = tool 'sonar-scanner'
        SONAR_HOST_URL = 'http://192.168.33.10:9000'
    }
    
    stages {
        stage('Checkout from GitHub') {
            steps {
                echo '📥 Récupération du code depuis GitHub...'
                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/main']],
                    userRemoteConfigs: [[
                        url: 'https://github.com/oussama433/mon-jenkins-test.git',
                        credentialsId: 'github-cred'
                    ]]
                ])
                echo '✅ Code récupéré!'
            }
        }
        
        stage('Build with Maven') {
            steps {
                echo '🔨 Construction du projet avec Maven...'
                sh './mvnw clean package -DskipTests'
                echo '✅ Build Maven réussi!'
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                echo '🔍 Analyse de la qualité du code avec SonarQube...'
                echo "URL SonarQube: ${SONAR_HOST_URL}"
                
                // Utilisation du token sonarqube-token
                withCredentials([string(credentialsId: 'sonarqube-token', variable: 'SONAR_TOKEN')]) {
                    echo "✅ Token SonarQube disponible!"
                    
                    sh """
                        ${SCANNER_HOME}/bin/sonar-scanner \
                        -Dsonar.projectKey=student-project-jenkins-${BUILD_NUMBER} \
                        -Dsonar.projectName='Student Project' \
                        -Dsonar.sources=src \
                        -Dsonar.java.binaries=target/classes \
                        -Dsonar.host.url=${SONAR_HOST_URL} \
                        -Dsonar.token=${SONAR_TOKEN}
                    """
                }
                echo '✅ Analyse SonarQube soumise !'
            }
        }
        
        stage('Wait for Analysis') {
            steps {
                echo '⏳ Attente de la fin de l\'analyse SonarQube...'
                sleep 30
                echo '✅ L\'analyse devrait être terminée.'
                echo "📊 Vérifiez sur: ${SONAR_HOST_URL}"
            }
        }
        
        stage('Build Docker Image') {
            steps {
                echo '🐳 Construction de l\'image Docker...'
                
                // Créer un Dockerfile si inexistant
                sh '''
                    if [ ! -f Dockerfile ]; then
                        echo "Création du Dockerfile..."
                        cat > Dockerfile << DOCKERFILE
FROM openjdk:17-jdk-slim
COPY target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app.jar"]
DOCKERFILE
                    fi
                '''
                
                // Construction avec credentials Docker Hub
                withCredentials([usernamePassword(
                    credentialsId: 'docker-hub-cred',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh """
                        docker build -t ${DOCKER_IMAGE}:${BUILD_NUMBER} .
                        docker tag ${DOCKER_IMAGE}:${BUILD_NUMBER} ${DOCKER_IMAGE}:latest
                        echo "✅ Images taggées:"
                        echo "   - ${DOCKER_IMAGE}:${BUILD_NUMBER}"
                        echo "   - ${DOCKER_IMAGE}:latest"
                    """
                }
                echo '✅ Image Docker construite !'
            }
        }
        
        stage('Push Docker Hub') {
            steps {
                echo '📤 Pushing image to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'docker-hub-cred',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh """
                        echo ${DOCKER_PASS} | docker login -u ${DOCKER_USER} --password-stdin
                        docker push ${DOCKER_IMAGE}:${BUILD_NUMBER}
                        docker push ${DOCKER_IMAGE}:latest
                        echo "🎉 Images poussées avec succès!"
                    """
                }
                echo '✅ Images poussées vers Docker Hub !'
            }
        }
    }
    
    post {
        success {
            echo '🎉🎉🎉 PIPELINE RÉUSSI ! 🎉🎉🎉'
            echo '================================'
            echo '✅ Toutes les étapes complétées:'
            echo '   1. Checkout GitHub'
            echo '   2. Build Maven'
            echo '   3. Analyse SonarQube'
            echo '   4. Build Docker Image'
            echo '   5. Push Docker Hub'
            echo ''
            echo '🔗 Liens utiles:'
            echo "   - SonarQube: ${SONAR_HOST_URL}"
            echo '   - Docker Hub: https://hub.docker.com/r/oussama433/student-project'
            echo "   - Image: ${DOCKER_IMAGE}:${BUILD_NUMBER}"
        }
        failure {
            echo '❌❌❌ PIPELINE ÉCHOUÉ ❌❌❌'
            echo '================================'
            echo '🔧 Vérifiez:'
            echo '   1. Que SonarQube est démarré'
            echo '   2. Le token sonarqube-token est valide'
            echo '   3. Les credentials docker-hub-cred sont corrects'
            echo '   4. Docker est installé et en cours d\'exécution'
        }
        cleanup {
            echo '🧹 Nettoyage...'
            sh 'docker system prune -f || true'
        }
    }
}
