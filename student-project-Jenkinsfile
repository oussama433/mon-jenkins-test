pipeline {
    agent any
    
    environment {
        DOCKERHUB_CREDENTIALS = credentials('docker-hub-cred')
        DOCKER_IMAGE = 'oussama433/student-project'
        SCANNER_HOME = tool 'sonar-scanner'
        // Définir explicitement les variables SonarQube
        SONAR_HOST_URL = 'http://192.168.33.10:9000'
        SONAR_TOKEN = credentials('sonarqube-token')
    }
    
    stages {
        stage('Checkout from GitHub') {
            steps {
                echo '📥 Récupération du code depuis GitHub...'
                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/main']],
                    userRemoteConfigs: [[
                        url: 'https://github.com/oussama433/mon-jenkins-test.git',
                        credentialsId: 'github-cred'
                    ]]
                ])
            }
        }
        
        stage('Build with Maven') {
            steps {
                echo '🔨 Construction du projet avec Maven...'
                sh './mvnw clean package -DskipTests'
                echo '✅ Build Maven réussi!'
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                echo '🔍 Analyse de la qualité du code avec SonarQube...'
                echo "URL: ${SONAR_HOST_URL}"
                echo "Token disponible: ${SONAR_TOKEN != null ? 'OUI' : 'NON'}"
                
                // Utiliser les variables explicites SANS withSonarQubeEnv
                sh """
                    ${SCANNER_HOME}/bin/sonar-scanner \
                    -Dsonar.projectKey=student-project-jenkins \
                    -Dsonar.projectName='Student Project' \
                    -Dsonar.sources=src \
                    -Dsonar.java.binaries=target/classes \
                    -Dsonar.host.url=${SONAR_HOST_URL} \
                    -Dsonar.token=${SONAR_TOKEN}
                """
                echo '✅ Analyse SonarQube soumise !'
            }
        }
        
        stage('Quality Gate') {
            steps {
                echo '⏳ Attente du résultat de la Quality Gate...'
                // Pour waitForQualityGate, on utilise withSonarQubeEnv
                withSonarQubeEnv('SonarQube-Server') {
                    timeout(time: 5, unit: 'MINUTES') {
                        waitForQualityGate abortPipeline: true
                    }
                }
                echo '✅ Quality Gate passée !'
            }
        }
        
        stage('Simuler Build Docker') {
            steps {
                echo '🐳 [SIMULATION] Construction de l\'image Docker...'
                echo "Image: ${DOCKER_IMAGE}:${BUILD_NUMBER}"
                echo '✅ Étape Docker simulée'
            }
        }
        
        stage('Simuler Push Docker Hub') {
            steps {
                echo '📤 [SIMULATION] Pushing image to Docker Hub...'
                echo '✅ Push simulé vers Docker Hub'
            }
        }
    }
    
    post {
        success {
            echo '🎉 Pipeline exécuté avec succès!'
            echo '📊 Étapes complétées:'
            echo '   ✅ Checkout GitHub'
            echo '   ✅ Build Maven'
            echo '   ✅ Analyse SonarQube'
            echo '   ✅ Quality Gate'
        }
        failure {
            echo '❌ Pipeline a échoué.'
            echo '🔍 Debug info:'
            echo "   - SONAR_HOST_URL: ${SONAR_HOST_URL}"
            echo "   - SONAR_TOKEN défini: ${SONAR_TOKEN != null ? 'OUI' : 'NON'}"
        }
    }
}
